export enum ChatRoleEnum {
  system = 'system',
  user = 'user',
  assistant = 'assistant',
}

export enum BotCommands {
  start = 'start',
  new = 'new',
}

export enum BotMessageType {
  voice = 'voice',
  text = 'text',
}
